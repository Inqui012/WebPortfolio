헤딩태그 계층구조 계획하기.
VS코드 html 기본구조 에밋 ! 안나올 경우는 html:5 선택?

<p> </p>
문단. 긴 글.

<b> </b>
<strong> </strong>
글자를 굵게. 강조. b의 경우는 의미가 없지만 strong 태그의 경우는 의미강조역할을 한다.

<i> </i>
<em> </em>
이태릭체. i는 의미없음, em은 의미강조.
i태그는 아이콘으로 사용하는일이 많은가.

<u> </u>
밑줄.

<br/>
한줄 띄우기. 엔터역할. 혼자서 태그를 열고 닫는 셀프클로징 태그.

<mark> </mark>
색넣기...? 형광펜 효과?

<sup> </sup>
<sub> </sub>
위, 아래 첨자. 수식같은데 쓰는 첨자 입력하는 느낌.

<s> </s>
<del> </del>
취소선. s태그의 경우는 기존의 데이터와 취소된 데이터가 무관련일때 사용되고
del은 두 데이터가 연관이 있을 때 사용된다. 기존의 데이터를 인식할 필요가 있느냐의 차이?

<ins> </ins>
del 태그와 묶여서 많이 쓰이는듯? 새롭게 업데이트 된 텍스트. 밑줄이 자동으로 생긴다.

<small> </small>
<big> </big>
글의 일부를 작거나 크게 표현한다. 블록형이 아니라서 태그 끝에 텍스트를 넣어도 출력된다.
big 태그의 경우 현재는 지원을 종료해 사용하지 않는다.

<hr/>
html4에서는 그저 구분선을 넣는정도였지만 5에서는 주제가 바뀐다는 의미로 사용된다.
셀프 클로징 태그.

<blockquote> </blockquote>
<cite> </cite>
인용문 태그. 자동으로 들여쓰기가 되는듯.
하위 설정으로 태그 안에 cite 값을 설정할 수 있고 이를 cite태그로 사용할 수 있다.

<q> </q>
들여쓰기.
한 줄 짜리 짧은 인용문의 경우에는 이쪽을 사용한다.

<pre> </pre>
프린터태그. 입력한 그대로 출력한다. 이 태그 사이의 내용은 작성한 그대로 나온다.
자주 안쓰임. 실무에서는 절대 안쓰임.



*주석처리 단축키 shift alt a

예제(2_h.html)의 경우 b의 입장에서는 p태그가 부모이고. p입장에서는 b태그가 자식태그.
부모 > 자식 > 자손 태그순으로 내려가는듯
* em은 강조된 태그, strong은 중요한 태그. html의 태그는 설명이 추상적인 경우가 많다.
때문에 사용할때에 스스로 의미부여를 하는편이 좋다? 사용구분을 지어놓는 느낌인가?
* html4 에서 사용하던것들이 html5로 올라오면서 의미가 바뀐경우가 있다.
* 아이콘 svc
* html 코드상의 엔터는 적용되지 않음.



* list tag
엄청 많이 사용하게 된다. 중요?

<ul> </ul>
순서가 정해져있지 않은 리스트를 만들때 사용. unordered list.

<li> </li>
list item. ul태그 이나 ol 태그 안에서 자식태그로 사용된다. 
ul태그의 자식으로는 li태그만 들어올 수 있으나 li태그 안에는 다시 ul태그가 들어갈 수있다.
속성으로는 type 이 있다. circle, disc, square 목록표현식의 모양을 결정한다.
현재는 이 속성은 사용되지 않고 css로 대체한다.

<ol> </ol>
order list 순서가 정해져있는 목록


* 에밋 사용할때 그냥 목록에서 선택하지 말고 * 기호를 사용할 수 있다.
예시로는 li*5 일경우 li태그 5개가 자동생성된다.

* 크롬 개발자도구. f11